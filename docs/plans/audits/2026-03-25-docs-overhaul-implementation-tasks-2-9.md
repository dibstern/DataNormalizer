# Tasks 2-9: Docs Overhaul (README, DocFX articles, TOC) -- Audit Report

**Summary:** The plan's code examples are largely accurate against the actual generated code. The JSON property names, container property names, and type suffixes match the source generator's behavior. However, there are several instances where the plan's instructions are insufficiently specific about stale `Normalized` references that must be updated, one missing file update, and a few minor correctness issues in documentation examples.

## Findings

| # | Category | Action | Issue | File:Line | Amendment / Question |
|---|----------|--------|-------|-----------|----------------------|
| 1 | Incorrect Code | **Amend Plan** | **Task 2 JSON: `result` DTO should use camelCase property names.** The plan's JSON example (lines 383-386) shows `"routesIndices"`, `"originIndex"`, `"destinationIndex"` inside the `"result"` object. These are correct camelCase renderings of the CLR names `RoutesIndices`, `OriginIndex`, `DestinationIndex`. **However**, the plan's JSON example (lines 388-392) shows `"name"` and `"hopsIndices"` for RouteDto, which is correct. All JSON names verified correct against `EmitterHelpers.ToCamelCase()`. | Plan lines 382-407 | No change needed -- verified correct. |
| 2 | Implicit Assumptions | **Amend Plan** | **Task 5: Plan doesn't explicitly mention removing `TeamList` references.** The existing `getting-started.md` line 98 shows `result.TeamList[0]` and line 100 shows `result.TeamList // NormalizedTeam[] (typed array)`. The plan says to replace `result.TeamList[0]` with `result.Result` (correct), but doesn't explicitly say to REMOVE the `result.TeamList` line entirely. Since Team is root and NOT referenced by other types, there is no `TeamDtos` property on the container at all. The plan's replacement code at lines 616-619 correctly omits TeamDtos, but the instructions at lines 599-601 don't call out the removal. | `docs/articles/getting-started.md:100` | Amend Task 5 Step 1 to add: "Remove the `result.TeamList` line entirely -- the root type does not get a list property unless it is referenced by other types." |
| 3 | Implicit Assumptions | **Amend Plan** | **Task 6: Multiple stale `Normalized` references not explicitly called out.** The plan's Task 6 Step 1 says "Update type names to use `Dto` suffix" but doesn't enumerate the specific locations. Configuration.md has stale references at: line 50 (`Normalized{TypeName}` class), lines 87-88 (`NormalizedTeamResult`, `NormalizedOrderResult`), line 96 (`Normalized{RootType}Result`), and lines 100-105 (all `PersonList`/`AddressList`/`TeamList` and `NormalizedTeam`/`NormalizedPerson`/`NormalizedAddress`). | `docs/articles/configuration.md:50,87-88,96,100-105` | Amend Task 6 Step 1 to explicitly list ALL lines needing updates: (1) line 50: `Normalized{TypeName}` -> `{TypeName}Dto`; (2) lines 87-88: `NormalizedTeamResult` -> `TeamResultDto`, `NormalizedOrderResult` -> `OrderResultDto`; (3) line 96: `Normalized{RootType}Result` -> `{RootType}ResultDto`; (4) lines 100-105: Replace entire Container Result API code block to use `result.Result`, `result.PersonDtos`, `result.AddressDtos` (remove `TeamList` since root not referenced). |
| 4 | Missing Wiring | **Amend Plan** | **`docs/api/index.md` not updated in any task.** Line 9 references `Normalized{RootType}Result` which should be `{RootType}ResultDto`. No task (2-9) touches this file. | `docs/api/index.md:9` | Add a step to one of the tasks (Task 8 is the most logical fit, since it updates `docs/index.md`) to also update `docs/api/index.md` line 9: change `Normalized{RootType}Result` to `{RootType}ResultDto`. |
| 5 | Incorrect Code | **Accept** | **Task 2 JSON: transport example container name.** The plan at line 335 says `result.RouteDtos` for collections. Verified: `GetListPropertyName` for `Route` with default naming returns `ToPlural("RouteDto")` = `"RouteDtos"` (CLR). JSON camelCase = `"routeDtos"`. Both correct. | Plan line 335 | -- |
| 6 | Incorrect Code | **Accept** | **Task 4: Container example uses short type names.** The plan's generated container (lines 523-535) shows `SearchResponseDto Result`, `RouteDto[] RouteDtos`, etc. without namespace prefixes. The actual emitter uses fully-qualified names like `Ns.SearchResponseDto`. This is fine for documentation -- it's a simplified illustration. | Plan lines 523-535 | -- |
| 7 | Implicit Assumptions | **Amend Plan** | **Task 6: `result.TeamList` in Container Result API section needs nuanced update.** The current configuration.md lines 101-105 show `result.TeamList[0]` as root access, `result.TeamList` as a typed array, plus PersonList and AddressList. The plan says to update `result.TeamList[0]` -> `result.Result`, but Team (as root not referenced by others) does NOT have a `TeamDtos` list at all. The replacement code must also explain this: "The root entity is always available via `result.Result`. Non-root types get typed array properties (e.g., `result.PersonDtos`). The root type only gets an array if other types reference it." | `docs/articles/configuration.md:100-105` | Amend Task 6 Step 1 to include explanation text about root property behavior, matching the description in Task 4 section 7. |
| 8 | Fragile Code | **Accept** | **Task 8 (landing page): No specific JSON provided.** The plan gives high-level guidance ("flat container with `result`, typed `Dto` arrays, integer refs") but no concrete JSON. This gives the implementer flexibility but risks inconsistency with Task 2's example. | Plan lines 789-796 | -- |
| 9 | Incorrect Code | **Accept** | **Task 7: DN1002 message says "Only the last value will be used" but plan says "Remove the duplicate call. Only one JSON name can be assigned per type."** The actual diagnostic message (DiagnosticDescriptors.cs:56) says the last value wins, implying the build succeeds. The plan's resolution says to remove the duplicate. Both are valid -- the diagnostic is an Error severity but generation still proceeds. The documentation is fine as written. | `DiagnosticDescriptors.cs:56` vs Plan line 766 | -- |
| 10 | Implicit Assumptions | **Amend Plan** | **Task 5: getting-started.md line 85 uses `Normalized{TypeName}` pattern.** The plan says to update "the 'What the source generator produces' section" with `{TypeName}Dto` instead of `Normalized{TypeName}` (line 623). But it doesn't explicitly mention updating line 85's bullet point text: `**\`Normalized{TypeName}\`** -- a partial class implementing \`IEquatable<T>\``. This should become `**\`{TypeName}Dto\`** -- ...`. The plan's line 622-623 does cover this semantically, but the specific bullet point at line 85 could be missed. | `docs/articles/getting-started.md:85` | Amend Task 5 to explicitly call out: "Update line 85 bullet: `Normalized{TypeName}` -> `{TypeName}Dto`." |
| 11 | Implicit Assumptions | **Accept** | **Task 5: getting-started.md line 93 uses `Normalized{RootType}Result`.** The plan's Task 5 instructions at line 624 say "Container type: `{RootType}ResultDto`", which covers this line. | `docs/articles/getting-started.md:93` | -- |
| 12 | Implicit Assumptions | **Amend Plan** | **Task 5: getting-started.md line 112 diagnostics range says "DN0001-DN0004".** After Task 7 adds DN1001 and DN1002 to the diagnostics article, this link text should be updated to "DN0001-DN1002" (or just "Compiler diagnostics"). Task 5 runs before Task 7, so this update should either be deferred to Task 7 or noted in Task 5 as a follow-up. | `docs/articles/getting-started.md:112` | Add to Task 7 (or Task 5): "Update diagnostics link text in getting-started.md from 'DN0001-DN0004' to 'DN0001-DN1002'." |
| 13 | Implicit Assumptions | **Amend Plan** | **Task 6: configuration.md line 117 says "DN0001 and other diagnostics" -- should this also mention DN1001/DN1002?** After Task 7 adds the new diagnostics, the cross-reference in configuration.md could be updated. But since Task 6 runs before Task 7, this could be handled in Task 7. | `docs/articles/configuration.md:117` (at end: "See Diagnostics Reference...") | Add to Task 7: "Update cross-references in configuration.md and getting-started.md to include DN1001-DN1002 in their diagnostic ranges." |
| 14 | Fragile Code | **Accept** | **Task 2: Benchmark numbers are placeholders (XXX KB).** The plan correctly notes these need to be filled in from Task 1's output. This is fine as long as Task 1 is completed first. | Plan lines 417-423 | -- |
| 15 | Incorrect Code | **Accept** | **Task 4 line 574: Priority order is correct.** Verified against `TypeGraphAnalyzer.ResolveJsonNameOverride` (line 674-693): config (`PropertyJsonNameOverrides`) is checked first, then `[NormalizeJsonNameAttribute]`, then falls back to null (default camelCase in emitter). | `TypeGraphAnalyzer.cs:674-693` | -- |
| 16 | Implicit Assumptions | **Accept** | **Task 4 line 489 uses `ContainerSuffix` default "Dto" description.** Verified against `NamingModel.cs:9` and `NamingBuilder.cs:21`: default is `"Dto"`. Correct. | `NamingModel.cs:9` | -- |
| 17 | Missing Wiring | **Ask User** | **README.md "How It Works" section (lines 125-135) has extensive stale references.** Line 129: `Normalized{TypeName}`, line 131: `Normalized{TypeName}Result`, `{TypeName}List`, "root entity always at index 0", line 133: `Denormalize(Normalized{T}Result)`. The plan says to REPLACE the entire README, so technically all of this goes away. But the plan should make sure the replacement README's equivalent section (if any) uses the new naming. The plan's "How It Works" equivalent is not detailed in Task 2. | README.md:125-135 | Is the "How It Works" section intentionally omitted from the new README? The plan structure (lines 326-339) focuses on "What DataNormalizer Does" and "Quick Start" but doesn't have a dedicated technical explanation section like the current "How It Works". |
| 18 | Incorrect Code | **Amend Plan** | **Task 6: "Multiple root types" section uses stale names.** Lines 87-88 of configuration.md show `// -> NormalizedTeamResult` and `// -> NormalizedOrderResult`. These need to become `// -> TeamResultDto` and `// -> OrderResultDto`. The plan's generic instruction "Update type names to use `Dto` suffix" covers this in principle but is easy to miss since it's in a comment, not code. | `docs/articles/configuration.md:87-88` | Amend Task 6 Step 1 to explicitly list: "In 'Multiple root types' section, update comments: `NormalizedTeamResult` -> `TeamResultDto`, `NormalizedOrderResult` -> `OrderResultDto`." |

## Summary of Required Actions

### Must Amend (7 findings):
- **#2**: Task 5 -- explicitly note removal of `TeamList` line (root without back-references has no list)
- **#3**: Task 6 -- enumerate ALL stale `Normalized` references in configuration.md
- **#4**: Add `docs/api/index.md` update to Task 8 (or create a new step)
- **#7**: Task 6 -- add explanation of root property behavior in Container Result API section
- **#10**: Task 5 -- explicitly call out updating line 85 bullet
- **#12**: Task 7 -- update diagnostics range in getting-started.md from DN0001-DN0004 to DN0001-DN1002
- **#18**: Task 6 -- explicitly call out comment updates in "Multiple root types" section

### Ask User (1 finding):
- **#17**: Is the "How It Works" section intentionally omitted from the new README design?

### Accept (8 findings):
- **#1, #5, #6, #8, #9, #11, #14, #15, #16**: All verified correct or informational

**No issues found in:** Task 3 (gzip article), Task 7 (diagnostics -- verified DN1001/DN1002 exist and match), Task 9 (TOC -- straightforward addition of 2 entries)

**Overall assessment:** The plan's code examples are accurate. The main risk is incomplete updates to stale `Normalized` references scattered across existing documentation files. The plan covers the spirit of these changes but needs more explicit enumeration to prevent implementers from missing individual occurrences.
